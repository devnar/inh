function add(bir,iki) {
    echo(Number(bir) + Number(iki))
}

function subtract(bir,iki) {
    echo(Number(bir) - Number(iki))
}

function divide(bir,iki) {
    echo(Number(bir) + Number(iki))
}

function multiply(bir,iki) {
    echo(Number(bir) + Number(iki))
}
